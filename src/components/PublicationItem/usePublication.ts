import { MouseEvent } from "react"

const usePublication = (): ReturnType => {
    const onClick = (e: MouseEvent<HTMLDivElement>) => {
        
    }

    return {}
}

type ReturnType = {

}

export default usePublication;

